extends layout 

block content 
    h1= title 
    p Are you sure you want to delete this item?

    | #{item.name} 
    | #{item.description} 
    | #{item.category.name}
    | #{item.stock}

    form(method='POST')
            div.form-group
                input#bookid.form-control(type='hidden', name='itemid', value=item._id )

            button.btn.btn-primary(type='submit') Delete